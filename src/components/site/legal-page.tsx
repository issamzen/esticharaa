import { FileText, Mail, ShieldCheck } from "lucide-react";
import { SiteLayout, PageHeader } from "./layout";
import type { LegalPageContent } from "@/i18n/legal-content";

export function LegalPage({content}:{content:LegalPageContent}){
 return <SiteLayout><PageHeader eyebrow={content.eyebrow} title={content.title} description={content.intro}><span className="inline-flex items-center gap-2 rounded-full border border-border/70 bg-background/70 px-4 py-2 text-xs text-muted-foreground shadow-soft"><FileText className="size-3.5 text-secondary"/>{content.lastUpdated}</span></PageHeader><section className="mx-auto grid max-w-7xl gap-8 px-4 py-12 sm:px-6 lg:grid-cols-[270px_1fr] lg:py-16"><aside className="lg:sticky lg:top-24 lg:self-start"><div className="premium-card p-4"><p className="flex items-center gap-2 px-2 text-xs font-bold uppercase tracking-wider text-secondary"><ShieldCheck className="size-4"/>{content.toc}</p><nav className="mt-3 max-h-[62vh] space-y-1 overflow-y-auto">{content.sections.map(section=><a key={section.id} href={`#${section.id}`} className="block rounded-xl px-3 py-2 text-xs leading-5 text-muted-foreground transition hover:bg-muted hover:text-foreground">{section.title}</a>)}</nav></div><a href="mailto:support@estichara.ma" className="mt-3 flex items-center gap-2 rounded-2xl border border-border/60 bg-card p-4 text-xs text-muted-foreground transition hover:border-secondary/30 hover:text-secondary"><Mail className="size-4 shrink-0"/><span>{content.contact}</span></a></aside><article className="premium-card divide-y divide-border/60 overflow-hidden px-5 sm:px-8">{content.sections.map(section=><section key={section.id} id={section.id} className="scroll-mt-28 py-7 sm:py-9"><h2 className="text-xl font-semibold sm:text-2xl">{section.title}</h2><div className="mt-4 space-y-3 text-sm leading-7 text-muted-foreground sm:text-base sm:leading-8">{section.paragraphs.map((paragraph,index)=><p key={index}>{paragraph}</p>)}{section.bullets&&<ul className="space-y-2 ps-1">{section.bullets.map((bullet,index)=><li key={index} className="flex gap-3"><span className="mt-3 size-1.5 shrink-0 rounded-full bg-secondary"/><span>{bullet}</span></li>)}</ul>}</div></section>)}</article></section></SiteLayout>;
}
